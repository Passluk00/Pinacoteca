/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface AssegnaAreaACuratore$Params {
  idArea: number;
  idCur: number;
}

export function assegnaAreaACuratore(http: HttpClient, rootUrl: string, params: AssegnaAreaACuratore$Params, context?: HttpContext): Observable<StrictHttpResponse<{
}>> {
  const rb = new RequestBuilder(rootUrl, assegnaAreaACuratore.PATH, 'post');
  if (params) {
    rb.query('idArea', params.idArea, {});
    rb.query('idCur', params.idCur, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      }>;
    })
  );
}

assegnaAreaACuratore.PATH = '/admin/assegnaAreaACuratore';
