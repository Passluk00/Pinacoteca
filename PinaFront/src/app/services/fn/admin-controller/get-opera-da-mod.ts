/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OperaFront } from '../../models/opera-front';

export interface GetOperaDaMod$Params {
  idOpera: number;
}

export function getOperaDaMod(http: HttpClient, rootUrl: string, params: GetOperaDaMod$Params, context?: HttpContext): Observable<StrictHttpResponse<OperaFront>> {
  const rb = new RequestBuilder(rootUrl, getOperaDaMod.PATH, 'get');
  if (params) {
    rb.query('idOpera', params.idOpera, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OperaFront>;
    })
  );
}

getOperaDaMod.PATH = '/admin/getOperaDaMod';
